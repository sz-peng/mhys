# OpenAI API 配置
# 如果配置了环境变量，将优先使用环境变量，否则使用前端设置中的配置

# OpenAI API Key (必填)
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI API Base URL (可选)
# 默认值: https://api.openai.com/v1
# 如果使用第三方转发服务（如 DeepSeek、OhMyGPT 等），请修改此项
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI Model (可选)
# 默认值: gpt-3.5-turbo
# 可选值: gpt-3.5-turbo, gpt-4, gpt-4-turbo-preview, deepseek-chat 等
OPENAI_MODEL=gpt-3.5-turbo

# ============================================================
# 数据库配置 (可选 - 设置后启用后台管理功能)
# ============================================================

# PostgreSQL 连接字符串 (Supabase Session Pooler)
# 设置此变量将启用：数据库存储、后台管理面板
# 如不设置，数据仅存储在用户浏览器的 localStorage 中
# DATABASE_URL=postgresql://postgres.xxx:[PASSWORD]@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres

# 管理员密码 (当 DATABASE_URL 存在时必填)
# 用于登录后台管理面板 /admin
# ADMIN_PASSWORD=your-secure-password

# Session 加密密钥 (当 DATABASE_URL 存在时必填)
# 至少 32 个字符的随机字符串
# ADMIN_SESSION_SECRET=your-random-32-character-secret-key
